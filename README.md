# Bugrov Leaks Archive Viewer

**[UA]** Веб-інтерфейс для перегляду архіву злитого листування (експорт Telegram). Проект дозволяє зручно читати чати, переглядати медіа-галереї, використовувати пошук та аналітику в офлайн-режимі або через веб-сервер.

**[EN]** A web-based viewer for Telegram chat exports. Features include a responsive UI, media gallery, search, analytics, and offline capability.

---

## 🏗 Архітектура

Проект складається з двох частин:

1. **`build.py`** — Python-скрипт, який парсить HTML-експорт Telegram і генерує оптимізовану статичну версію сайту в папку `site/`.
2. **`frontend/`** — вихідний код веб-інтерфейсу (HTML, CSS, JS), який копіюється в `site/` під час збірки.

## 🚀 Швидкий старт

### Вимоги

- Python 3.8+
- `pip install beautifulsoup4 lxml`

### Запуск (для розробки / перегляду)

1. Відкрийте файл `site/index.html` у будь-якому сучасному браузері.
   - **Примітка:** Для роботи `serviceWorker` (PWA) та коректного завантаження JSON-даних (через fetch) у деяких браузерах (Chrome) може знадобитися локальний веб-сервер.

   ```bash
   # Запуск локального сервера (опціонально)
   cd site
   python -m http.server 8000
   # Відкрийте http://localhost:8000
   ```

### Оновлення даних (збірка)

Якщо ви додали нові чати або змінили код у `frontend/`, запустіть збірку:

```bash
python build.py
```

Це оновить папку `site/`.

## 📂 Структура проекту

```text
.
├── build.py            # Головний скрипт генерації
├── frontend/           # Вихідний код інтерфейсу
│   ├── css/            # Стилі (style.css, media_gallery.css)
│   ├── js/             # Логіка (app.js)
│   ├── index.html      # Шаблон головної сторінки
│   └── profiles.json   # Метадані профілів (фото, імена)
├── site/               # ГЕНЕРОВАНА папка (готовий сайт)
│   ├── data/           # JSON-чанки з повідомленнями
│   ├── media/          # Фото, відео, голосові
│   └── index.html      # Вхідна точка
└── requirements.txt    # Залежності Python
```

## ⭐ Можливості

- **Адаптивний дизайн:**
  - 📱 **Мобільна версія:** повноцінний інтерфейс (додаток) з меню-шторкою (overlay), свайпами та повноекранними модальними вікнами.
  - 💻 **Десктоп:** класичний вид Telegram Web.
- **Офлайн-доступ (PWA):** можливість встановити як додаток на телефон/ПК.
- **Медіа-галерея:** перегляд усіх фото/відео/аудіо в чаті.
- **Пошук:** глобальний (по всіх чатах) та локальний (всередині чату).
- **Аналітика:** статистика активності учасників.
- **Безпека:** вхід за паролем (опціонально), дисклеймер 18+.

## ⚠️ Дисклеймер

Цей архів містить матеріали, що можуть бути образливими або неприйнятними (18+). Проект створено виключно з метою документування та дослідження суспільно значущої інформації.

Автор коду інтерфейсу не несе відповідальності за зміст листування.
Першоджерело: [@bugrovleaks](https://t.me/bugrovleaks/3)
